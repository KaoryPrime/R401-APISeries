<Page
    x:Class="WinUIApp.Views.AddSeriePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Spacing="10" Padding="20" MaxWidth="500" HorizontalAlignment="Center">
        <TextBlock Text="Ajouter une série" Style="{StaticResource TitleTextBlockStyle}" HorizontalAlignment="Center"/>

        <TextBox Header="Titre" Text="{x:Bind ViewModel.SerieToAdd.Titre, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <TextBox Header="Résumé" Text="{x:Bind ViewModel.SerieToAdd.Resume, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                 AcceptsReturn="True" Height="100"/>

        <NumberBox Header="Nb Saisons" Value="{x:Bind ViewModel.SerieToAdd.Nbsaisons, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SpinButtonPlacementMode="Inline"/>

        <NumberBox Header="Nb Épisodes" Value="{x:Bind ViewModel.SerieToAdd.Nbepisodes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SpinButtonPlacementMode="Inline"/>

        <NumberBox Header="Année création" Value="{x:Bind ViewModel.SerieToAdd.Anneecreation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SpinButtonPlacementMode="Inline"/>

        <TextBox Header="Chaîne (Network)" Text="{x:Bind ViewModel.SerieToAdd.Network, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <Button Content="Ajouter" Command="{x:Bind ViewModel.AddCommand}" HorizontalAlignment="Stretch" Margin="0,20,0,0"/>
    </StackPanel>
</Page>